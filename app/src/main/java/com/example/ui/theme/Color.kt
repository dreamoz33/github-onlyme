package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light Mode Premium Colors (Matching the provided design screenshot)
val CleanLightBg = Color(0xFFF2F3F8)      // soft lavender sky daylight white background
val PureWhiteCard = Color(0xFFFFFFFF)     // clean card background
val TextDarkPrimary = Color(0xFF121215)   // dark charcoal text for high contrast readability
val TextDarkSecondary = Color(0xFF6B7280) // neutral slate gray subtext
val ModernRoyalBlue = Color(0xFF4D49FF)   // vibrant royal blue/indigo for active accents and switches

// Dark Mode Premium Colors (Strictly Black background and White active accents)
val BlackBg = Color(0xFF000000)           // pure pitch black background
val DarkCardSurface = Color(0xFF161618)   // dark neutral carbon surface for cards
val CosmicSecondarySurface = Color(0xFF28282D) // dark accents / actions row
val HighContrastWhite = Color(0xFFFFFFFF) // white accent indicating active clocks/dots/+ buttons
val MoonWhite = Color(0xFFF9FAFB)         // elegant off-white text
val DimSilver = Color(0xFF9CA3AF)         // gray text labels

val CoralError = Color(0xFFFF5252)        // red accents for danger/exceptions
